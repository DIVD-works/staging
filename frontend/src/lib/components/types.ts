export type FooterLinks = {
    href: string,
    name: string,
};

export type Card = {
    title: string,
    description: string,
    icon: string,
};

export type Team = {
    source: string,
    title: string,
    description: string,
};

export type LegalInfo = {
    title: string,
    description: string,
};
